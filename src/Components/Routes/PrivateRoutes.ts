import Dashboard from "../Template/DashBoardTemplate/DashboardTemplate";

const PrivateRoutes = [
  {
    elements: [
      {
        path: "/",
        component: Dashboard,
      },
    ],
  },
];

export default PrivateRoutes;
