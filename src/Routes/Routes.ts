import Dashboard from "../Components/Molecule/Dashboard/Dashboard";

const PrivateRoutes = [
  {
    elements: [
      {
        path: "/dashboard",
        component: Dashboard,
      },
    ],
  },
];

export default PrivateRoutes;
